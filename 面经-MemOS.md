# MemOS 项目面经（AI 应用方向）

> 口径边界：本文依据仓库 `main` 提交 `6706a10` 生成。提交记录显示开发由同一个人的两个 Git 身份完成，但正式简历中的“个人独立完成”仍建议由本人确认。Features 0–5 已发布；Feature 6 评测基础设施已发布，真实模型四基线结果仍为 `NOT RUN`。

## 1. 项目简介（简历可用）

MemOS 是面向跨会话 AI Agent 的长期记忆基础设施，围绕“该记什么、事实如何演进、怎样安全召回与彻底删除”建立了受治理写入、异步可靠处理、时态版本、混合检索和可复现评测链路。项目以 Java 25 / Spring Boot 实现生产边界，以 PostgreSQL + pgvector + FTS 承载权威数据和可重建检索投影，以 Python 统一四类基线的评测证据。

## 2. 简历 bullet

- **受治理的 AI 记忆写入：** 针对 LLM 输出可能漂移、越权、夹带敏感信息或把短期噪声持久化的问题，完成 provider-neutral 结构化提取与确定性写入策略：对 schema、大小、信任上限、敏感性和写入权限逐层校验，并对拒绝/待审核内容做无原文落库；已通过 17 例确定性机制测试，真实模型准确率待评测。
- **异步请求可靠性：** 针对模型调用延迟高、可重复执行且应用可能在任意崩溃点退出的约束，将源事件与工作意图在 PostgreSQL 中原子提交，以语义幂等键、批量领取、数据库时间租约、fencing token、心跳和有界重试隔离旧 worker；故障、并发、重放与远程 CI 已验证当前边界。
- **时态事实状态建模：** 针对原地覆盖丢失历史、单纯追加又积累矛盾的问题，完成稳定事实谱系、不可变断言版本和追加转移模型，区分事件时间、有效时间与事务时间，用乐观锁和数据库约束处理更新、回填、冲突、校正和失效；已通过 14 例状态机 fixture 与 PostgreSQL 故障测试。
- **多信号证据检索：** 针对向量相似无法单独解决精确标识、时间意图、真值状态与权限的问题，实现 vector、FTS、结构化和时态四路独立召回，在硬隔离/可见性过滤后以 RRF 融合，可选重排在超时、模型身份或候选不完整时回退到确定性顺序；真实质量与延迟收益待四基线试验。
- **上下文安全与隐私治理：** 针对长期记忆导致的跨会话投毒、越权读取和删除后复活风险，将召回内容作为显式不信任证据转义并按完整渲染结果做 token 预算，同时以验签身份、角色边界、立即隐藏、租约隔离擦除和不透明墓碑形成可验证的 PostgreSQL 内闭环。
- **可复现评测工程：** 针对 AI 项目容易把手工样例、不公平 token 预算或被删除的失败样本当成效果证据的问题，建立了冻结中英双语数据、四基线统一答案路径、模型 digest 校验、显式失败行、用量/存储计量与不可手改派生报告；运行器已发布，选定模型结果仍为 `NOT RUN`。

## 3. 面试问题

### 主题一：项目价值与 AI 写入治理

#### 1. 为什么长期记忆不是“Embedding + Vector DB + TopK”？

**第一人称口播：** 我做这个项目时先看到一个现象：跨会话 Agent 真正出问题的地方，往往不是找不到语义相似的文本，而是把噪声当事实、把过期信息当当前状态、把别人的数据召回进来，或者在删除后又被旧任务重建。我因此把任务定义为一个生命周期：先保留证据，再生成候选，经过写入政策后建立版本化事实，最后在权限、时间和 token 预算内选取证据。这个改造的结果是每个错误都有明确的状态和验证位置，但我不会把机制完整性说成已经证明质量胜出，因为正式基线实验还没运行。

**追问 1：Memory 和 RAG 的边界在哪里？**

**第一人称口播：** 我会把 RAG 理解为用外部知识支持当前问题的检索增强机制，而长期记忆还要回答谁的信息、是否值得持久化、新事实与旧事实的关系、什么时候有效、如何更正或删除等状态管理问题。在项目中，我把检索当成记忆生命周期的一个读阶段，而不是整个系统；写入前有信任和敏感性门禁，写入后有版本与删除契约，召回时还有授权和真值过滤。这样做能让我对每个决策找到权威证据，也能在检索策略失败时单独修复，而不会破坏事实历史。

**追问 2：为什么不保存所有对话？**

**第一人称口播：** 我把“保存所有对话”看成证据保留策略，而不是自动形成有用记忆的策略。对话里包含客套话、短期状态、模型猜测、工具页面的恶意指令和敏感信息，全部变成可召回状态会放大未来每一次决策的噪声和攻击面。我在实现中先区分源证据与派生记忆，模型只提出结构化候选，确定性政策再判断是否持久化。结果是拒绝、忽略和待审核都成为可测试的状态，而不是靠提示词中一句“请不要记噪声”。真实误写率仍需用标注数据测量。

#### 2. 为什么不让 LLM 直接写入记忆？

**第一人称口播：** 我在写路径中把 LLM 定位为“语义结构提案者”，因为它适合从自然语言中抽取主体、谓词、值和时间，却不适合掌握授权、幂等、事务和破坏性操作。我的任务是在模型输出和权威存储之间建立一条不可跳过的确定性边界：先做有界解析与严格结构校验，再结合来源信任、写入能力、敏感性、置信度和新颖性决定处置。这使得即便模型给出极高置信度，也不能绕过敏感信息拒绝或工具来源的信任上限。已有 fixture 验证这些机制，但模型语义准确率还需真实数据评测。

**追问 1：JSON Schema 已经校验通过，为什么还不够？**

**第一人称口播：** 我把 schema 校验定义为“形状正确”，它可以证明字段存在、类型正确、枚举合法、大小和嵌套没有超过边界，但它不能证明内容是真的、来源有权限、值得长期保留或者不含敏感信息。所以我在结构校验之后还加了信任上限、敏感性联合检测、权限和新颖性判断，而且模型标注不能降级确定性检测出的风险。最终的工程结果是“能解析”与“能写入”被分成两个独立合同，任何 provider 更换都不会自动改变业务授权规则。

**追问 2：如何处理敏感候选？**

**第一人称口播：** 我的原则是敏感性判断必须 fail closed，不能因为模型说“不敏感”就覆盖确定性检测结果。实现中我把模型分类与应用检测做联合，根据类别选择拒绝、限制、脱敏、标记化或待审核；若所需的标记器不可用，系统宁可不写，也不降级为原文落库。我还特别检查拒绝后的候选、隔离区、日志、指标和错误信息，确保测试中的秘密标记不会在旁路保留。这证明了当前机制，但不代表分类器在真实数据上已达到某个准确率。

#### 3. 你如何保证 provider 可替换而领域规则不漂移？

**第一人称口播：** 我遇到的核心约束是 AI provider 变化很快，如果领域模型直接依赖 SDK、HTTP 响应或 Spring 组件，模型更换会连带改变业务规则和测试。我把提取、embedding、rerank、token 计数、时钟和 ID 生成都抽象为由消费模块定义的端口，适配层只负责连接 provider 和把失败转换为类型化结果，授权、状态转移和破坏性操作留在确定性代码中。为了验证，我用无凭证 fake 覆盖 CI，同时对真实本地模型校验完整 digest、能力和维度。这样能将 provider 风险限定在适配边界，但仍需对模型版本迁移实现投影对账。

**追问 1：为什么要校验模型 digest，仅记 tag 不行吗？**

**第一人称口播：** 我在评测设计中发现，模型 tag 常常是可变的部署名称，同一 tag 在不同时间可能指向不同权重或能力，如果只记 tag，就无法证明两次运行真的使用了同一快照。所以我把请求标签与持久化来源身份分开，启动时核对完整 SHA-256 digest 和声明能力，作业也绑定不可变版本。当旧作业的版本与当前 worker 不一致时，我让它在调 provider 之前永久失败，避免用新模型假冒旧来源。这样产生的是可核验身份链，但不代表模型输出质量已经达标。

**追问 2：为什么领域层不引入 Spring？**

**第一人称口播：** 我希望领域层表达的是记忆的纯业务不变量，例如哪些状态转移合法、单值事实如何处理时间重叠、旧版本如何保留。如果它依赖 Spring、JDBC 或持久化注解，单测就会被容器和数据库捆绑，而且适配层容易反过来拥有政策。我通过 Maven 模块依赖和架构测试强制领域层保持纯 Java，再由外层实现事务和远程调用。结果是我可以用确定性 fake 高速验证状态机，再用 PostgreSQL 集成测试单独证明约束和并发语义。

### 主题二：异步可靠性与事务

#### 4. 为什么选择事务 Outbox，而不是同步调模型或直接写队列？

**第一人称口播：** 我面对的是一条既要保存对话证据、又要调用慢且可失败模型的写路径。如果同步调用，对话延迟与 provider 可用性被绑定；如果数据库提交后再发队列，进程可能在两步之间崩溃，留下没有后续处理的已接收事件。我因此在同一 PostgreSQL 事务里写入源证据与持久工作意图，请求提交后立即返回，worker 再从数据库领取。这个设计的可验证结果是：提交前崩溃则两者都没有，提交后即使无 worker 也保留待处理状态。代价是最终一致和额外作业管理，具体 freshness 仍待测。

**追问 1：Outbox 是否保证 exactly-once？**

**第一人称口播：** 我不会把 outbox 说成端到端 exactly-once，因为 worker 在外部调用完成后、写入成功状态前可能崩溃，新 worker 依然可能再次调用 provider。我实际保证的是 at-least-once 执行下的幂等权威效果：每个源、请求和语义工作意图都有稳定唯一键，提交时还要校验当前租约 token，重放只能得到原有逻辑结果。在故障测试中，处理器可以被多次调用，但数据库中不会出现第二份逻辑版本或下游意图。这个口径比笼统说“绝对一次”更准确，也暴露了 provider 需自身幂等的剩余风险。

**追问 2：如果后续接 Kafka，当前设计是否作废？**

**第一人称口播：** 我把 PostgreSQL outbox 当作 MVP 的权威工作记录，而不是永久排斥消息平台。当前工作类型、语义键、重试状态和处理器端口都是显式合同，如果未来已经测到轮询对数据库的压力、消费者数量或跨系统订阅需求，可以增加一个中继器把已提交 outbox 发往 Kafka，下游仍以原语义键幂等。我当前没有这样做，是因为尚无规模数据证明值得增加新的一致性、部署和删除边界。因此这是一条有测量触发条件的演进路径，不是预先堆中间件。

#### 5. Lease 和 fencing token 分别解决什么？

**第一人称口播：** 我在故障模型中考虑了一个常见场景：worker A 领取后卡住，租约过期，worker B 重新领取并成功处理，此时 A 又恢复并尝试提交。Lease 只解决“一段时间内谁可以处理”，过期后允许工作被回收；fencing token 才是最终提交权限，每次领取都换新 token，成功、失败或续租都要用作业、拥有者、token、未过期状态做比较并交换。我还使用数据库时间，避免应用主机时钟偏差决定权限。集成测试已验证旧 token 更新零行，所以过期 worker 无法覆盖新结果。

**追问 1：为什么还需要心跳续租？**

**第一人称口播：** 我加心跳不是为了取消 fencing，而是为了减少慢 provider 调用和串行批次中的可避免重复工作。一个作业可能需要多次 embedding，同批后面的作业还在等前面执行，如果只给固定短租约，即使 worker 健康也可能被另一个实例回收。我在批次返回时就为每个已领取作业开始续租，按租期三分之一尝试从数据库当前时间延长，并在写终态前停止且等待在途续租。续租失败时仍以最终 token fence 为准，所以它是减少浪费的优化，不是 exactly-once 保证。

**追问 2：重试如何避免风暴？**

**第一人称口播：** 我先把失败分成可恢复与永久失败，例如超时、429 和部分 5xx 进入有界重试，而配置错误、模型身份漂移、响应形状错误则直接进入隔离终态。对可恢复失败，我使用最大尝试次数和上限指数退避，下次可执行时间持久化，而不是当前线程立即自旋。尝试用尽后进入 dead 状态，需要在排除根因后显式 replay，不做无限自动重试。这个方案已用注入时钟做确定性测试，但带 jitter 的大规模调度策略要等真实负载后再引入。

#### 6. 如何设计幂等键，为什么一个键不够？

**第一人称口播：** 我把重复拆成三种语义：客户请求重试、同一业务源再次到达，以及下游作业重复执行。客户幂等键用来识别同一次 API 操作，源标识用来识别同一个业务事件，语义作业键则绑定某个版本化处理意图。我还对不可变请求内容做规范化指纹，完全一致的重试返回原身份，同键但内容不同则拒绝为冲突。这个结构的结果是网络重试不会创建第二份证据，作业 replay 也不会创建第二份权威版本，同时不会把非法键复用当成正常重试。

**追问 1：如果响应丢失，客户怎么办？**

**第一人称口播：** 我把“数据库已提交但 HTTP 响应丢失”当成必须支持的正常故障，而不是异常边角。客户用同一幂等键和同一不可变请求再次提交时，数据库唯一约束先定位原记录，服务再对比规范化指纹；若完全一致，返回原源事件和原工作作业身份，并标记为 duplicate，而不是再插入。如果请求内容发生改变，则返回冲突且不修改旧记录。这样客户可以安全重试，并继续用稳定作业 ID 查询异步链路状态，不需要猜测首次请求是否成功。

**追问 2：为什么指纹不能作为删除后的墓碑？**

**第一人称口播：** 我在隐私设计中区分了运行期幂等指纹和删除后非内容墓碑。请求指纹是由原始内容派生的，对低熵信息，攻击者可以枚举候选内容并比对 hash，所以它不等于匿名化，也不应该在治理删除后保留。我的擦除流程会清除 payload 和内容派生指纹，仅保留由随机不透明 ID、操作状态、策略依据和时间构成的非内容审计与墓碑。这些墓碑用来阻止旧作业和旧身份恢复数据，但对 backup、WAL 和外部 provider 的擦除仍属于未建立边界。

### 主题三：时态事实与并发状态

#### 7. 你如何表示“现在住杭州，以前住上海”？

**第一人称口播：** 我没有用一行可变文本表示这类事实，因为把“上海”直接改成“杭州”会让历史问题无法回答，也丢失修改原因和来源。我先用租户、用户、Agent、主体和谓词定位稳定事实谱系，再在其下追加不可变版本，每个版本保留有效时间和来源。新住址在时间上晚于旧住址且不重叠时，追加一个更新转移，把旧版本标记为历史，新版本标记为当前。这样当前查询和历史查询都有确定答案，而且可以按事务时间回看系统在某一刻实际知道什么。

**追问 1：Event time、valid time 和 transaction time 有什么不同？**

**第一人称口播：** 我会用一次搬家说明三个时间：event time 是用户说的搬家事件何时发生，valid time 是“居住于某地”这个事实在现实世界中适用的半开区间，transaction time 是系统何时将该版本和转移写入权威库。它们在延迟上报或回填时不会相同：用户今天告诉我去年的搬家，系统今天才知道，但事实从去年就有效。我的 as-of 视图按 transaction time 重建，不伪造系统在去年已经知道这件事；同时版本内保留 valid time，便于回答去年住在哪里。

**追问 2：时间不确定时如何处理？**

**第一人称口播：** 我不会把“去年夏天”这类不精确文本强行变成某一天，因为这会让后续的更新顺序带上虚假精度。我在版本中同时保留原始时间文本、精度、可解析的上下界和置信度，使用半开区间避免端点歧义。对单值事实，只有在时间关系可以确定时才做更新或回填；区间重叠、缺失或无法比较时，系统安全地保留冲突，不随意选一个当前值。这保证了状态机不编造确定性，但自然语言日期解析质量仍是待验证项。

#### 8. 你如何区分更新、冲突、并存和重复？

**第一人称口播：** 我先根据业务谓词确定基数，例如主居地是单值，喜欢的菜系可以是集合值，这个策略由确定性代码给出，不让新值自己选规则。进入同一谱系后，内容完全一致或确定性改写被视为重复强化，不新建版本但增加来源；集合值的不同值可并存；单值在时间不重叠时按新旧做更新或回填；重叠、缺失或无法排序则标记冲突。这些转移都在纯状态机中先产生计划，数据库事务再用锁版本和约束原子落库。

**追问 1：为什么重复事实不直接忽略？**

**第一人称口播：** 我把重复内容和重复证据区分开。三次表达同一偏好不需要三个事实版本，否则会放大召回权重并浪费上下文预算；但三个独立来源仍有证据价值，也有助于日后判断该事实是否被反复确认。所以我对确定性重复执行 reinforcement 转移，不创建新断言版本，却追加来源关联和状态转移证据。当语义是否改写无法确定时，不应该用宽松相似度强行合并，而要保留独立候选或冲突。目前 fixture 证明了三个改写保留一个版本和三个来源，真实改写识别率仍待测。

**追问 2：模型给出的 relation 为什么不直接决定转移？**

**第一人称口播：** 我允许模型提出“可能是更新”或“可能是重复”的语义关系，但它只是输入提示，不能直接将历史事实设为失效。原因是转移会影响未来召回，属于权威状态和潜在破坏性操作；若模型被投毒或时间推理错误，直接采信会覆盖真实历史。我让确定性状态机根据谱系身份、基数、去重结果和时间关系重新计算，无法证明时转入冲突，而不是随意选赢家。结果是转移可以用纯单测、数据库约束和追加证据共同审计。

#### 9. 校正和失效为什么需要乐观锁与现有证据？

**第一人称口播：** 我把人工校正和失效当成比普通记忆提取更高风险的变更，因为它们会改变当前真值状态。请求必须带当前谱系锁版本，数据库只有在存储版本与预期版本一致时才提交，这使两个并发校正中只有一个成功，另一个获得前置条件失败。同时，客户不能直接携带一段绕过政策的替换文本，而是先通过正常提取与写入策略获得受治理候选，再在校正中引用它。最终以追加失效转移和新版本保留历史，不做原地覆盖，已通过并发与重放测试。

**追问 1：为什么不直接传 replacement value？**

**第一人称口播：** 我如果允许校正接口直接接收任意 replacement value，就会创建一条绕过提取 schema、敏感性、信任和写入权限的后门，这与前面建立的治理边界相矛盾。所以我设计成两步：用户先把校正证据当成新源事件提交，等待它完成提取和策略判定，校正接口再只引用同一授权 scope 下的源和已批准候选。这样多一次异步往返，但保住了统一的策略、来源和审计合同。我会在产品上显式展示这个处理中状态，而不是为了低延迟破坏治理边界。

**追问 2：如何保证校正重试不新增第二次转移？**

**第一人称口播：** 我为每次校正或失效要求客户稳定幂等键，并对目标版本、证据源、候选和原因等不可变输入生成请求身份。在事务中，先申领这个身份，再检查谱系乐观锁，成功时将幂等结果、新版本、状态转移、来源和当前投影一起提交。响应丢失后，完全相同的请求会返回原逻辑结果，不再推进锁版本；同键不同内容则冲突；不同键但持有旧锁版本则前置失败。这三层边界共同覆盖网络重试和真正的并发冲突。

### 主题四：混合检索与上下文组装

#### 10. 为什么要做四路独立召回，而不是先向量 TopK 再过滤？

**第一人称口播：** 我在设计读路径时，把查询分成语义改写、精确词项或标识、主体/谓词结构和历史时间意图。向量检索对改写很有用，但精确项可能因为语义稀释而不在 TopK，历史事实也可能被当前相似事实挤掉。如果其他检索只能在向量 TopK 之后过滤，候选已经不可恢复。我因此在相同 scope、真值状态和时间条件下独立查询 vector、FTS、structured 和 temporal 候选，再用名次融合。这使每路的候选和贡献可追踪，但哪组合在真实语料上更好仍要用冻结数据消融验证。

**追问 1：授权和真值状态应该由 reranker 处理吗？**

**第一人称口播：** 我不会让 reranker 决定授权或真值状态，因为它的任务是在已可见候选中优化相关性顺序，而不是控制数据边界。如果把跨租户、已删除、过期或失效事实先放进候选，再希望模型把它们排低，就等于用一个概率输出实现安全策略，而且诊断路径已经暴露数据。我在 SQL 候选阶段就应用租户/用户/Agent 等值、活跃生命周期、状态和目标时间硬过滤，Java 策略再做一次防御性检查。重排失败只会影响可见候选的顺序，不会改变权限、状态或权威数据。

**追问 2：会话型输入也要检索吗？**

**第一人称口播：** 我没有默认对每一句用户输入都检索长期记忆，因为寒暄、简单确认或与任务无关的轮次会生成额外 embedding 调用、数据库负载和无关上下文，反而干扰模型。我在检索服务前设置确定性 query gate，对可明确判定不需要记忆的输入直接跳过，并在 trace 中记录低基数决策原因，不记查询原文。对于边界模糊的场景，当前是规则型解析器，优点是可复现和低成本，缺点是召回准确性可能不足，需要在基线实验中把 gate 开关作为消融项。

#### 11. 为什么选 RRF，它有什么缺点？

**第一人称口播：** 我选 RRF 的前提是当前没有足够标注数据去校准向量余弦分数、FTS 相关度、结构化命中和时态命中。这些原始分数尺度不同，直接加权求和意味着手写了一套没有证据的校准。RRF 只使用每路名次，某个候选在多路靠前时会自然累积优势，也容易记录组件贡献和重现。它的缺点是丢弃分数幅度，第一名险胜和遥遥领先被同等对待，固定常数也可能不适合所有查询。所以我把 RRF 定位为低假设基线，不声称它已是最优融合。

**追问 1：如何验证 RRF 真比其他方案好？**

**第一人称口播：** 我会先冻结开发集与测试集，对每个查询保存各路候选、名次、过滤原因和最终证据，在相同权限、语料、K 值和 token 预算下比较向量单路、向量+词法、增加时态/结构化、RRF、校准加权和可选重排。指标不只看最终答案，还要分开看 Recall@K、MRR、完整证据召回、上下文精度、延迟和 provider 用量。参数只能在开发集调整，测试集不允许检查后回改。当前项目已具备这个运行器和证据包，但还没有真实模型结果，因此面试时我会把它说成验证计划而非收益结论。

**追问 2：Reranker 失败时怎么降级？**

**第一人称口播：** 我把 reranker 设计成可选优化层，所以它必须在失败时不破坏已可用的确定性候选顺序。调用前我传入类型化候选、固定模型版本和绝对 deadline；返回后校验模型身份、候选 ID 是否完整且唯一，不允许模型新增、遗漏或替换候选。遇到超时、provider 错误、版本漂移、重复 ID 或集合变化时，整个重排结果被丢弃，直接返回原 RRF 顺序，而不做部分采信。这保证了可用性与身份完整性，但重排是否值得额外延迟和成本仍需实验。

#### 12. 你如何在 token 预算内安全组装记忆上下文？

**第一人称口播：** 我把召回结果看成待审查证据，而不是可以直接拼进 system prompt 的指令。在组装时，所有记忆文本和元数据先做 XML 转义，统一放在一个显式标记为不信任数据的容器中，并附带不可伪造的版本和源证据身份。为避免一个事实的多个近似版本占满预算，通常每个谱系只取一个，明确冲突时才保留两个备选。每尝试新增一项，我都对包含根标签、分隔符和所有项的完整候选上下文计 token，超限则截止。这使返回结果可重现地不超预算，但真实模型投毒抵抗还需红队评测。

**追问 1：为什么不能把每个片段 token 数相加？**

**第一人称口播：** 我最初需要解决的是 Java 运行时与 Python 基线的预算口径不一致。Tokenizer 并不保证连续文本的 token 数等于各片段独立计数之和，根标签、属性、分隔符和相邻字符都可能影响分词。如果一边按片段估算，另一边对完整上下文计数，最终的质量对比就不再是等预算实验。所以我让 Java 在每次尝试加入证据时都渲染完整容器，再使用与 Python 基线相同的 digest-pinned embedding tokenizer 计数，同时把额外计数调用和输入 token 记入用量。运行器还会独立复核最终上下文，避免服务自证。

**追问 2：XML 转义是否就能解决 Prompt Injection？**

**第一人称口播：** 我不会声称 XML 转义能解决一般性 Prompt Injection。它能验证的是结构边界：记忆中的闭合标签、角色伪造或工具命令在渲染后仍是单一不信任容器下的文本，不会改写结构或变成额外消息。但模型仍可能在语义上遵循这段恶意文本，因此还需要写入时的来源信任、敏感与程序性记忆权限，读取时的指令/数据分离，以及固定模型的 write-to-use 红队测试。当前四例恶意字符串 fixture 只证明结构转义，行为免疫仍未建立。

### 主题五：安全治理与可复现评测

#### 13. 为什么“删除记忆”不是简单 DELETE 一行？

**第一人称口播：** 我在设计删除时先列出了数据的所有去向：源 payload、内容指纹、提取候选、断言版本、当前状态、向量/FTS 投影、未完成作业、重放记录和审计事实。只删某一张表会留下可召回副本，旧 worker 甚至可在删除后重建投影。我因此把删除实现为状态机：请求事务首先将对象立即设为不可见并停止相关作业，异步 worker 再在数据库租约隔离内原子清除内容与派生数据，保留非内容审计和不透明墓碑。故障与并发测试已验证 PostgreSQL 内边界，backup、WAL 和外部 provider 仍需部署级方案。

**追问 1：为什么要先立即隐藏，再异步擦除？**

**第一人称口播：** 我需要同时满足用户可见契约和可恢复操作两个目标。如果把所有权威、投影和作业清理都放在一个 HTTP 请求中，任何超时都会让客户无法判断已删到哪里，后续也难以安全重试。所以我在请求事务中先改变生命周期、删除检索投影和 checkpoint、终止相关未完成作业，使后续所有读、召回、materialization 和 replay 都立即因非活跃状态而失败。物理擦除由可重试 worker 在单一租约事务内执行，中途租约丢失则整个回滚。这样用户先得到确定不可见语义，系统再完成可观测终态。

**追问 2：如何防止删除后复活？**

**第一人称口播：** 我不只在读路径过滤已删数据，还在每个可能重建它的入口检查生命周期和墓碑。删除请求会使源与谱系变为非活跃，未完成提取和投影作业被终止；旧 worker 即使已经完成远程调用，提交时也需同时通过租约 token、最新转移水位和活跃状态校验，因此更新零行。最终擦除还写入不含内容的随机墓碑，后续 replay 或重用旧身份时会被拒绝。并发的用户级删除与新写入还共享 scope 锁，确保要么新写先提交并被包含在擦除中，要么删除先提交且新写被阻止。

#### 14. 你如何证明评测是公平且可复现的？

**第一人称口播：** 我先承认 AI 评测最容易出现的问题是实验后调整测试集、各基线获得不同上下文预算、模型 tag 漂移、失败样本被删除，以及手工修改派生报告。我的任务是把这些风险变成机械化门禁：数据、分割、问题截止时间、提示词、schema 和模型 digest 都写入 manifest 并做 hash；四个基线使用同一答案模型、输出合同和证据 token 上限；每个问题与重复次数必须有一行成功、失败或排除记录。所有指标、用量、存储和 Markdown 报告都由验证器独立重生成并逐字节比对。这证明了运行器完整性，但还没有产生真实模型结果。

**追问 1：为什么 fixture 的 100% 不能写进简历当质量指标？**

**第一人称口播：** 我把确定性 fixture 的作用定义为验证已编码的机制是否按合同工作，例如旧 token 是否被拒绝、重叠时间是否进入冲突、恶意闭合标签是否被转义。这些用例由开发者按预期状态构造，样本少、分布不代表真实用户，甚至某些检索排名就是手工构造候选。因此 14/14 或 6/6 只能表述实现与预定合同一致，不能推导出提取准确率、召回质量、延迟或生产安全性。我在仓库中将这些数值明确标为非正式 benchmark，简历中只作为测试覆盖证据，不宣称业务收益。

**追问 2：为什么失败样本也必须进入分母？**

**第一人称口播：** 我如果只对成功返回的问题计算准确率，一个经常超时、解析失败或在困难题上拒绝执行的系统反而可以通过删掉坏样本获得更高分数。所以我在运行前就根据基线、问题和重复次数展开完整执行身份集，验证器要求每个身份恰好一行，成功、失败或排除都必须显式存在。失败和排除使用不含用户内容的稳定错误类，但仍保留在指标分母和失败报告中。这样可用性问题不会被质量报告隐藏，也能区分模型答错与系统没有完成链路。

#### 15. 当前项目最大的未完成项和下一步是什么？

**第一人称口播：** 我会主动说明当前最大缺口不是又少一个中间件，而是还没有对预先声明的真实模型四基线完成合格运行。目前数据集、提示词、模型 digest、上下文预算、失败计数、用量、存储和报告验证器都已实现并通过远程 CI，但在真正运行前还需要补齐 embedding 模型版本变更时的投影 reconciliation，避免旧投影因版本过滤而静默缺失。然后在干净提交上执行预定 dev smoke，审核完整 artifact，只有验证器通过后才更新结果页。在此之前，我不会提供延迟、准确率或收益数字。

**追问 1：为什么模型版本变更需要投影 reconciliation？**

**第一人称口播：** 我把向量和 FTS 当成可重建投影，每行都带生成它的 embedding 版本和维度。检索时为了避免在不兼容空间中比较向量，必须硬过滤当前配置的版本和维度。如果部署只更换模型，却没有扫描权威谱系并为需要重建的对象产生新作业，旧投影会被正确过滤掉，但新投影永远不会出现，系统表现为静默漏召回。所以下一步要实现幂等对账：按当前活跃谱系与投影 checkpoint 差异生成可恢复作业，并在完成前暴露不完整状态，而不把投影缺失当成没有记忆。

**追问 2：你会什么时候引入专用向量库、OpenSearch 或图数据库？**

**第一人称口播：** 我不会为了架构名词在当前就引入三种专用存储，因为每新增一个系统都会带来双写、删除传播、备份、观测和一致性边界。我会先在预先定义的小、中、大负载上测 PostgreSQL 的过滤 Recall@K、p50/p95/p99、索引体积、写放大和 OLTP 争用；只有当可重现地不满足 SLO，且专用引擎的收益覆盖额外运维与治理成本时才拆分检索投影。图数据库还需要多跳/关系类任务的消融证明关系扩展确实带来显著增益。这保持当前架构简单，也给未来演进留下以数据为触发条件的路径。

## 4. 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
|---|---|---|
| 项目边界与证据分级 | `README.md`、`docs/progress.md`、`docs/benchmark/results.md` | 项目简介、Q1、Q14、Q15 |
| 模块图与依赖倒置 | `pom.xml`、`architecture-tests/src/test/java/dev/memos/architecture/ModuleBoundaryTest.java` | Bullet 1–6、Q3 |
| 源事件幂等接收 | `modules/ingestion/src/main/java/dev/memos/ingestion/SourceIngestionService.java` 中 `ingest`、`semanticJobKey`、`requestFingerprint` | Bullet 2、Q4、Q6 |
| 工作租约与重试 | `modules/materialization/src/main/java/dev/memos/materialization/OutboxWorkerService.java` 中 `runOnce`、`process`、`handleFailure` | Bullet 2、Q4–Q6 |
| 慢调用心跳 | `modules/adapters/src/main/java/dev/memos/adapters/system/ScheduledJobLeaseHeartbeat.java` | Q5 |
| 候选提取协调 | `modules/materialization/src/main/java/dev/memos/materialization/CandidateExtractionService.java` 中 `evaluate` | Bullet 1、Q2、Q3 |
| 严格候选解码 | `modules/materialization/src/main/java/dev/memos/materialization/StrictCandidateProposalDecoder.java`、`modules/materialization/src/main/resources/schema/memory-candidate.v1.schema.json` | Q2 |
| 确定性写入治理 | `modules/governance/src/main/java/dev/memos/governance/DeterministicCandidateWritePolicy.java`、`DeterministicSensitivityDetector.java` | Bullet 1、Q1–Q3 |
| Provider 身份 | `modules/adapters/src/main/java/dev/memos/adapters/extraction/OllamaStructuredCandidateExtractionAdapter.java`、`OllamaEmbeddingAdapter.java` | Q3、Q15 |
| 时态纯状态机 | `modules/memory-domain/src/main/java/dev/memos/domain/temporal/TemporalTransitionPlanner.java` 中 `planMaterialization`、`planSingleValued`、`conflict`、`reinforce` | Bullet 3、Q7–Q9 |
| 时态权威与事务 | `modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcTemporalMemoryAuthority.java`、`V004__temporal_memory_authority.sql` | Bullet 3、Q7–Q9 |
| 投影建造与水位 | `modules/materialization/src/main/java/dev/memos/materialization/ProjectionBuildJobHandler.java`、`JdbcProjectionBuildStore.java`、`V005__hybrid_retrieval_projection.sql` | Bullet 4、Q10、Q15 |
| 混合召回与 RRF | `modules/retrieval/src/main/java/dev/memos/retrieval/HybridRetrievalService.java` 中 `retrieve`、`fuse`、`rerank`、`sources`、`visible` | Bullet 4、Q10–Q12 |
| 各路候选 SQL | `modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcRetrievalCandidateStore.java` | Q10、Q11 |
| 上下文组装与转义 | `modules/context/src/main/java/dev/memos/context/MemoryContextAssembler.java` 中 `assemble`、`renderItem`、`escape` | Bullet 5、Q12 |
| 身份与角色 | `applications/memos-api/src/main/java/dev/memos/api/security/SecurityConfiguration.java`、`JwtActorContextResolver.java`、`MemosRoles.java` | Bullet 5、Q10、Q13 |
| 治理删除应用层 | `modules/governance/src/main/java/dev/memos/governance/GovernedDeletionService.java`、`DeletionWorkerService.java` | Bullet 5、Q13 |
| 擦除事务与复活防护 | `modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcDeletionStore.java`、`V006__governed_erasure.sql` | Bullet 5、Q13 |
| 冻结数据与四基线 | `benchmark/datasets/memos-assistant-smoke/v1`、`benchmark/src/memos_benchmark/baselines.py`、`runner.py` | Bullet 6、Q14、Q15 |
| 不可手改证据包 | `benchmark/src/memos_benchmark/artifacts.py`、`metrics.py`、`docs/implementation/feature-6.md` | Bullet 6、Q14、Q15 |

## 5. 面试使用提醒

- 开场先讲“长期错误会跨会话放大”，再讲生命周期，不要从技术栈开始报菜名。
- 能讲实现和测试的地方用“已验证”；只有设计或小 fixture 的地方用“当前机制 / 待测”。
- 不宣称 exactly-once、生产级 Prompt Injection 防御、全链路法律擦除、百万级规模或任何未运行的质量/延迟数字。
- 如果“个人独立完成”与真实协作边界不符，请把所有“我完成”改为“我负责的部分是……”，并补充 PR / commit 边界。
