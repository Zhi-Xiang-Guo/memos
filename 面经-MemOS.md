# MemOS STAR 面经：主问、追问、反例与证据

更新：2026-09-07。目标：1–3 年 Java 后端 / AI 应用与 Agent 工程岗位。配套：[模块导学与岗位判断](导学-MemOS.md)。
飞书阅读：[STAR 完整面经](https://my.feishu.cn/wiki/QsiOwKuboi5Vt0kQW17c9WVjnQd)。
本文替代简略答案作为完整练习稿；[原题库](docs/interview/memos-grill.md)仍可用作速查。

## 1. 项目简介与口播规则

项目简介候选：面向 AI Agent 的受治理长期记忆参考实现，使用 Java/Spring Boot、PostgreSQL、pgvector
与异步 worker，处理证据、版本、检索、删除和可重建投影，并提供可机械复核的四基线评测与 Waku 接口适配。

第一人称是待本人核验的练习视角，不证明本人独立完成全部代码。使用“我实现/主导”前，必须确认自己
能够解释、修改并验证该部分，明确 AI 与上游协作边界。这里没有编造任职公司、线上事故或业务规模。

STAR 用法：S 说明实际场景；T 说明目标与约束；A 说明具体判断和动作；R 说明可核验结果、失败与限制。
概念题使用本项目作场景锚点；设计题中的 A 是拟采取的动作，R 是拟验证标准，不能把计划说成已交付。
面试时合并成自然段，通常先给一句直接答案，再展开 STAR，不需要朗读四个字母。

已知结果固定：冻结集 MemOS 0/30、三个简单基线各21/30，10 个独立问题各重复三次；
修复开发 smoke MemOS 3/3；Waku 真实后端 conformance 12/12。后两项不能替代冻结质量结论。

## 2. 简历候选支柱

职责字段先由本人补齐；以下是项目层面的候选描述，不能不经核验直接冒认为个人经历。

- **状态建模：** 针对聊天记录无法表达当前真值和冲突的问题，以来源证据、不可变版本与状态转移建模长期事实，支持确定性更新、失效和历史检索；真实语义分类质量仍需未见集验证。
- **请求可靠性：** 针对请求重试、worker 崩溃和模型慢调用，将持久工作意图与输入原子提交，以幂等、租约及提交 fencing 约束数据库效果；并发和故障机制经测试验证，不承诺外部调用恰好一次。
- **权限与生命周期：** 针对跨主体读取和删除后重建风险，从验证后的身份推导数据范围，按事务隐藏、异步擦除和非内容墓碑执行治理；备份与 provider 删除另有部署边界。
- **检索与表示一致性：** 针对模型变更后的陈旧表示，按身份和构建代次限制候选，提供维护式重建与回滚；多通道排名融合与预算证据输出已有实现，收益尚待消融。
- **实验与消费合同：** 针对机制正确却端到端无效的问题，构建四基线原始数据与机械 verifier，保留冻结负结果；接入 Waku 并通过本地语义存储合同，进一步任务效用与生产规模待测。

## 3. 模拟面试契约

排序是依据项目风险与少量面经的教学优先级，不宣称真实题目频率。五个主题涵盖项目、可靠性、检索、
Memory 安全和评测交付，22 个主问题、25 个追问全部提供 STAR 口播。
L1 讲清输入输出；L2 能定位状态与失败；L3 能给反例和替代实验。单次模拟一次一问，先答再看稿。
评估只用“已验证、部分验证、未验证、矛盾”，不捏造精确总分。每题最多深追四层，重复空话两次就转入补基础。

## 4. 主题一：项目与系统边界

### Q1：用一分钟介绍 MemOS，为什么要做？

考察：业务动机与结果边界。证据：E01、E15。L1。

**STAR 主问口播**

S：我关注的是 Agent 跨会话重复使用旧事实、误记临时内容，以及用户删除后数据又被任务重建的问题。
T：我的目标是把长期记忆做成有来源、可纠正、可删除的状态系统，并验证它是否值得比简单方案更复杂。
A：我从写入、版本、读取和删除四条链路设计边界，把模型放在候选提取位置，让确定性代码负责授权与状态转移，再接四基线评测和 Waku 消费接口。
R：当前工程机制和本地接入有验证，但冻结结果为 MemOS 零比三十，简单基线各二十一比三十，所以我讲真实负结果、已定位问题和使用边界，不声称通用质量提升或生产规模。

#### 追问：没有效果优势，为什么还值得保留？

**STAR 追问口播**

S：如果项目只有复杂架构却没有证据，面试官质疑价值是合理的。我现在面对的正是机制实现与真实效用不一致。
T：我需要判断它适合做工程深度项目、可选模块，还是应该停止投入，不能用代码量替代业务价值。
A：我保留原始负结果，按写入、政策、检索与回答逐层定位问题，再用真实消费接口检验同步等待、更新和删除合同，明确哪些复杂性是治理需求带来的。
R：技术上可以弱化保留，重点讲可靠性和取舍；若用户任务中简单存储足够，MemOS 就作为可选后端。下一步需要任务验收和未见评测，不是继续添加没有验证收益的新功能。

### Q2：为什么选择 Java 模块化单体和 PostgreSQL？

考察：边界、备选方案与技术选择。证据：E02、E07。L1→L2。

**STAR 主问口播**

S：长期记忆同时涉及输入、异步任务、版本、权限和删除，如果一开始分散到多个存储，就会先遇到跨库一致性问题。
T：我希望在当前规模下把核心不变量放到容易验证的事务边界，同时保留替换模型或检索实现的能力。
A：我选择 Java 模块化组织领域和应用逻辑，API 与 worker 独立进程部署，PostgreSQL 保存权威事实和工作意图，向量与全文检索作为派生表示，外部模型通过端口接入。
R：这让数据库提交、幂等与删除更容易一起测试，代价是写事务和检索可能竞争同一数据库资源。现在没有负载证据支持拆库或增加消息平台，也不把单体解释为只能单进程运行。

#### 追问：既然选择新 Java，面试传统 Java 岗位怎么办？

**STAR 追问口播**

S：项目使用固定的新工具链，但很多业务团队已有自己的版本、框架和数据库，面试并不是比较谁用得更新。
T：我需要证明能迁移通用工程能力，而不是靠版本号显得先进，更不能未经验证就说项目兼容老版本。
A：我会优先解释接口、事务边界、唯一约束、线程与连接资源、异常传播和可测试性，再区分 PostgreSQL 与目标岗位数据库的行为差异。如果岗位要求旧版本，我先评估语言、依赖和部署约束，再决定是否另做兼容验证。
R：目前可承诺的是 Java 二十五和仓库 Wrapper 的复现结果；更早版本兼容、虚拟线程收益以及其他数据库迁移都没有证据，不会写成已经实现的优势。

### Q3：结构化 JSON 输出为什么仍不可信？

考察：结构、语义、授权三层分离。证据：E04、E05。L1→L2。

**STAR 主问口播**

S：真实模型可以输出完全符合 JSON 形状的候选，但其中日期范围、敏感性、来源或事实内容仍可能错误。
T：我需要让模型提案在进入长期状态前通过业务不变量，避免把格式成功当成事实正确或授权成功。
A：我分别做结构解码、语义区间检查和确定性政策判断。可信来源上限与角色来自外部已验证上下文，不能接受模型自称可信。遇到不合法时间、未知字段或政策拒绝，保留明确失败状态及受控诊断信息。
R：本地运行确实暴露过时间范围错误，严格解码正确拒绝了它；修复是收紧模型提案合同，而不是放松领域约束。有效 JSON 仍不是正确记忆，真实提取质量必须用标签评测。

#### 追问：自动修复 JSON、再多试几次不就好了？

**STAR 追问口播**

S：自动修复看起来能减少接口失败，但它可能偷偷改掉时间或事实含义，重试还会改变成本与成功率口径。
T：我需要区分纯传输错误、可确定修复的格式错误和无法安全猜测的语义错误，避免评测静默筛选成功样本。
A：我会在合同中声明重试范围、次数和超时，对语义不确定情况拒绝或隔离，并记录每次尝试的状态与用量。若允许格式修复，需要保存其规则版本并证明没有改变关键语义，不能静默交换日期边界。
R：现有失败样本保留，模型输出错误被纳入结果；未来新增修复器也要与直接失败方案比较，不能通过“重试到对”为原实验制造更好成绩。

## 5. 主题二：可靠写入与并发故障

### Q4：事务 Outbox 到底保证了什么？

考察：原子意图与至少一次执行。证据：E03、E06。L1→L2。

**STAR 主问口播**

S：接收源事件以后，应用可能在数据库提交和发出异步任务之间崩溃，造成有输入却永远没人处理。
T：我的目标是让已成功受理的事件具备可恢复的工作意图，同时避免慢模型调用长期占住数据库事务。
A：我把事件和任务记录放进同一事务，worker 后续独立领取并处理；模型调用在事务外，最终效果再用唯一约束、幂等和提交资格检查落库。客户端收到的是受理状态，可以查询完整物化进度。
R：这消除了输入与工作意图之间的双写窗口，崩溃后仍有持久任务可以接管。但外部模型调用可能重复，因此只能说明数据库效果收敛，不能声称整个系统实现了端到端恰好一次。

#### 追问：模型已经返回，提交前进程崩溃怎么办？

**STAR 追问口播**

S：这个位置最容易混淆，因为外部服务可能已消耗算力，而数据库还没有记住最终效果。
T：我需要保证恢复后既不丢处理机会，也不会把同一逻辑结果重复写成多个独立事实，并承认外部成本边界。
A：任务租约到期后由新 worker 接管，必要时重新调用模型；提交时重新校验任务所有权、幂等身份与当前代次。对 provider 若没有可查询的幂等合同，我不会假定能恢复上一次未落库响应，同时会区分已观测用量与未知调用。
R：当前机制能验证旧提交被阻断与重复效果不扩散，不能凭此证明零重复计费。若部署要求强对账，还需要 provider 请求身份及账单核验来补证据。

### Q5：lease、fencing、heartbeat 分别解决什么？

考察：旧 owner 恢复时的安全提交。证据：E06。L2。

**STAR 主问口播**

S：worker 可能暂时失去响应，系统需要允许其他进程接管，但旧进程恢复后仍可能拿着已经算出的结果写库。
T：我的目标是同时保证可恢复性和提交安全，而不依赖“旧进程应该已经死了”这样的假设。
A：租约规定临时处理期限，心跳为健康长任务续期，fencing 则在最终写入时验证当前 owner 与 token。数据库时间和条件更新决定提交是否仍有效，模型计算完成并不自动赋予写权限。
R：已有测试覆盖接管、旧 token、丢租与回滚。心跳减少不必要重复，却不能替代最后的提交 fence；批量领取后串行处理时，后面的任务也可能在开工前到期，不能只给当前正在调用模型的任务续租。

#### 追问：为什么不把租约设成一天，或者只加分布式锁？

**STAR 追问口播**

S：把租约拉得很长可以减少误接管，却会让真正宕机任务长时间无人处理；锁失效后旧持有者也未必停止计算。
T：我需要根据可观测任务时长平衡恢复速度和重复工作，同时保证任意过期执行者都不能重新发布结果。
A：我会先测排队与模型调用分布，再设置有界续租和失败回收，并始终在提交事务里验证所有权。超时、心跳失败、数据库连接问题分别记录，不能用无限延长锁掩盖故障。
R：现在能够证明的是机制上的旧 owner 拒绝，并没有代表负载下的最佳租约参数。没有这组分布就不宣称某个固定时长保证高可用，也不把分布式锁三个字当作安全提交的解释。

### Q6：幂等键与乐观并发控制有什么区别？

考察：重复请求、不同修改、业务身份。证据：E03、E07。L2。

**STAR 主问口播**

S：网络超时会让同一请求再次到达，两个人也可能基于同一旧事实同时做不同纠正，这两种问题不能共用一个判断。
T：我需要对重复请求返回稳定结果，对真正冲突的不同修改明确拒绝，避免表面成功但状态互相覆盖。
A：请求带稳定幂等键并绑定规范化内容指纹，同键异内容拒绝；修改现有谱系还要带预期版本条件，事务中检查当前版本。唯一约束负责并发重复竞争，版本条件负责基于过期快照的修改。
R：这些机制在不同层面收敛数据库效果。每次重试换新随机键会破坏原幂等意图，文本相似也不能代替请求身份；跨服务客户端的重试策略还需要额外合同验证。

#### 追问：删除后重放同一个幂等请求，应该返回旧成功吗？

**STAR 追问口播**

S：用户删除了事实以后，旧请求或旧任务仍可能被网络重试，单纯记住“这个请求曾经成功”不足以决定是否重新物化。
T：我需要同时保留可解释的重复处理语义和删除后的不可复活边界，不能让历史成功成为永久写授权。
A：我会让删除状态、墓碑和工作代次参与恢复路径检查，旧任务即使重新出现也不能绕过当前生命周期限制。请求回执与是否允许重建是不同问题，返回信息应按当前 HTTP 合同解释，不能自动创建新事实。
R：数据库内已有删除与 replay 防护机制；外部备份恢复和不同客户端创建的新请求身份仍需各自说明。幂等是在当前合法生命周期中收敛效果，不是对已删除内容承诺永久恢复。

## 6. 主题三：检索、上下文与运行诊断

### Q7：为什么使用混合检索与 RRF，它一定比向量检索好吗？

考察：候选来源、分数量纲与消融。证据：E08。L1→L3。

**STAR 主问口播**

S：用户可能用同义改写问偏好，也可能按精确端口或项目代号检索，单一路径未必覆盖两种情况。
T：我需要组合互补候选，同时避免直接相加不同检索器不可比较的原始分数，更不能先假设复杂方案一定更好。
A：我让各通道独立产生候选，先按具体断言版本去重，再对名次做倒数融合，之后按配置决定是否重排，并在最终上下文预算内选证据。当前查询与历史查询的候选通道也有区别。
R：机制和合成样例可以验证，但排名融合可能把噪声推高，相关性也不是事实有效性的证明。收益需要同数据、模型与预算的向量、混合和重排消融，目前没有完整证据支持“显著提升”。

#### 追问：重排超时了怎么办，deadline 就能保证请求准时返回吗？

**STAR 追问口播**

S：重排增加额外调用，如果 provider 阻塞或返回陌生身份，检索结果可能既慢又失去原候选约束。
T：我需要让可选增强失败时仍保持原有授权集合，并区分逻辑超时检查和真正中断外部调用。
A：当前服务在调用前后检查 deadline，对模型身份不一致、重复或缺失候选、异常结果回退到融合排名；重排必须返回输入候选的完整排列。对于真正的阻塞上限，还要由适配器配置网络超时及取消行为来验证。
R：已有回退机制不等于已证明所有 provider 都可被准时中断，而且现在没有完成真实重排器质量消融。面试时我会讲清这个边界，不把一个 deadline 参数包装成完整延迟保证。

### Q8：权限为什么要放在 TopK 之前？

考察：硬范围、近似检索与授权。证据：E08、E10。L2。

**STAR 主问口播**

S：若先从全库取相似度最高的若干条，再删除无权查看的结果，别人的数据会占掉名额，合法用户可能检索不到自己的证据。
T：我需要让每条候选路径都遵守可信身份范围，同时避免通过结果、计数或诊断信息泄露跨主体内容。
A：服务端从已验证 token 推导 tenant、user 和 agent，并在各路数据库候选截断前施加范围及可见性约束，最终合并后仍验证状态与模型身份。诊断接口另有角色要求，不由模型决定能看谁的数据。
R：这建立了应用与查询层的硬访问边界，但近似索引内部如何探索授权子集仍可能影响召回。安全过滤正确不等于 ANN 召回完美，也不能自动声称部署了完整数据库行级安全策略。

#### 追问：过滤后不足 K 条，能否扩大范围补足？

**STAR 追问口播**

S：授权范围很小或近似搜索召回不足时，结果少于请求数量是可能发生的，强行补齐容易把检索目标变成泄露数据。
T：我需要在不扩大访问权限的前提下评估候选探索、字面通道或精确搜索是否能改善合法结果覆盖。
A：我会保留 scope 不变，先区分库内本来就没有足够相关证据和近似搜索遗漏，再比较授权子集内的精确检索与近似路径，记录额外耗时和命中情况；必要时返回较少结果并允许拒答。
R：没有足够证据就不应该保证一定返回 K 条。扩大候选搜索预算可以作为待测优化，扩大 tenant 或用户范围不能作为降级方式，模型建议也不能改变这个授权边界。

### Q9：任务突然变慢，或者写完查不到，怎么排查？

考察：阶段化诊断，避免盲目优化。证据：E06、E09、E15。L2→L3。

**STAR 主问口播**

S：用户感知的是记忆迟迟不能用，但慢点可能在队列、模型冷加载、提取、提交、投影或上下文计数，API 很快并不能排除问题。
T：我需要用一个可证伪假设缩小范围，同时保留失败现场，不能一上来增加 worker 或更换模型。
A：我从请求身份关联 source 与 job，先看是否有候选和被接受版本，再看模型身份、代次、投影水位及最终选中证据。对慢路径拆分排队、调用、重试和数据库耗时，再核对连接资源、租约与机器负载。
R：项目已有任务状态与用量观察支持这条排查路线；代表负载的 freshness 分布仍未建立。我可以讲实际本地案例和下一项实验，不会把假设性的延迟问题说成已经解决的线上事故。

#### 追问：线程多一点、加缓存或换大模型是不是更快？

**STAR 追问口播**

S：并发提升可能同时增加模型争用、数据库连接等待与机器内存压力；缓存又会引入权限和删除失效问题。
T：我需要确认资源瓶颈与用户可接受延迟，再决定是否改变实现，而不是把扩容当作诊断本身。
A：我会在固定硬件与输入范围下记录每阶段时间、并发、队列长度和失败比例，比较受控并发变化。如果确需缓存，键必须包含模型身份、scope 和状态代次，并明确删除与纠正后的失效策略。
R：当前不能承诺扩大线程数、换大模型或加缓存一定改善，因为没有对应实验结果。已有机制也不等于已经选好生产连接池与并发参数，优化结论必须来自包含超时失败的完整分布。

## 7. 主题四：Memory、权限与安全

### Q10：为什么长期记忆不是保存全部聊天记录？

考察：选择、证据与治理。证据：E01、E05、E07。L1→L3。

**STAR 主问口播**

S：聊天包含猜测、转述、临时计划、旧地址和恶意指令，保留全部原文不能决定下一次任务应当采用哪条事实。
T：我需要同时保留可追溯证据与可控制的长期状态，避免信息量越大，错误越容易反复传播。
A：我把日志作为说过什么的证据，把长期记忆建模为经过选择、有来源、时间、范围和状态的断言。模型先提案，确定性规则负责是否接受及如何转移，检索再按当前任务约束选择可用证据。
R：这解释了版本、冲突与删除机制为什么存在，但它会增加误提取和维护成本。全文与摘要因此也是必要基线；在很小的历史或弱治理任务中，简单方案可能更合适，我不会只用上下文窗口不够来证明复杂记忆的价值。

#### 追问一：既然提取可能错，为什么不直接保存原文？

**STAR 追问口播**

S：提取会丢细节，保存原文有助于核验；但同一人先后给出两个地址时，检索出较旧原文仍可能让最终回答错误。
T：我希望保留证据追溯，又让系统能够表达纠正与失效，不能用全量保存替代状态治理。
A：我保持断言到来源的联系，保留期间追加版本和状态变化，对不确定语义保守处理，并分别测原文检索、提取接受和最终回答。治理删除时还必须同时检查原始证据与派生内容，而不是只清向量。
R：原文和结构状态是互补关系，不是抽取成功后就可以丢掉来源。当前真实分类准确率仍待更完整验证，若简单全文方案在目标任务上足够，我会减少复杂度而不是强迫所有数据进入结构化记忆。

#### 追问二：短期状态、滚动摘要、长期记忆如何配合？

**STAR 追问口播**

S：当前任务需要工具执行位置和中间结果，跨会话又可能需要偏好与项目约束，单一表示很难兼顾细节、成本和长期治理。
T：我需要按使用周期与责任边界分工，避免把所有摘要当成永久事实，也避免长期记忆承载整个运行时。
A：我让短期状态服务当前任务，摘要用于压缩已发生上下文，长期记忆维护有来源的稳定事实。恢复工具任务应该依赖运行时持久状态，而不是从摘要猜测工具是否执行过；长期事实变更则走版本和删除合同。
R：MemOS 当前重点验证长期事实子系统，Waku 的其余运行时能力需要另外审计。摘要可能丢证据，短期状态也不自动具备跨会话治理，这些取舍会作为不同基线和用户任务单独比较。

### Q11：如何处理事实更新、冲突和迟到事件？

考察：时间轴、事实身份、并发。证据：E07。L2→L3。

**STAR 主问口播**

S：系统九月先获知八月搬到苏州，几天后才收到六月住杭州的旧事件，如果按入库时间覆盖，就会倒退当前事实。
T：我需要区分事实何时有效和系统何时知道，并在无法证明顺序时保留不确定，而不是强行选最后收到的一条。
A：我先按范围、主体与谓词确定谱系，再区分单值与集合，结合有效区间判断替代、并存和冲突；纠正增加版本与转移，不改历史正文。并发修改还要带预期版本条件，数据库事务检查后才提交。
R：确定性时间和状态规则有测试，但自然语言日期、实体归一与冲突分类的真实质量仍待测。若模型把同一事实分成不同谱系，状态规则本身正确也可能留下旧信息，需要在上游归一层分析。

#### 追问一：两条互斥事实都没有可靠时间，能选置信度高的吗？

**STAR 追问口播**

S：模型可能对两条互斥断言都很自信，而且这类置信度未经标定，无法证明哪一条代表较新的真实状态。
T：我需要避免把概率式自述变成破坏性覆盖依据，保留证据并让消费方知道目前存在冲突。
A：我会先检查来源、是否有显式纠正关系以及有效区间能否建立顺序。如果无法证明，就按保守策略标记冲突或要求澄清，而不是用到达时间或模型分数强行裁决；读取时也必须把冲突状态显式表达。
R：这会牺牲一部分自动回答率，但有助于把不确定性变成可观察结果。是否过度保守需要用带标签冲突集测量，我不能把所有拒答都算安全成功，更不会把用户没有提供的日期补成事实。

#### 追问二：有两个时间字段，就算双时态数据库吗？

**STAR 追问口播**

S：很多项目保存创建时间和业务时间后就宣称支持任意历史查询，但“去年真实成立什么”和“系统截至去年知道什么”是不同问题。
T：我需要让接口定义与保留记录支持同一查询语义，避免用术语夸大已经实现的能力。
A：我会逐一检查有效时间区间、记录顺序、版本转移和 as-of 接口实际接受什么参数，再用迟到事件验证两个问题是否得到不同答案。只有能表达并重放两个维度，才考虑相应的完整双时态表述。
R：当前项目有版本、时间范围和历史接口的确定合同，但我不会仅凭字段名称声称支持任意双时态查询。对于尚未验证的历史视角，要明确范围并补例子，而不是让面试官靠猜测理解能力边界。

### Q12：如何防止越权、提示注入和删除后复活？

考察：身份、模型行为、生命周期三条边界。证据：E05、E10、E11。L2→L3。

**STAR 主问口播**

S：长期记忆可能被跨用户检索、携带恶意指令，或在删除后被旧任务重新构建，三者的成因与保证不同。
T：我需要让身份和破坏性动作由确定性服务控制，并为模型内容和持久生命周期建立可测试的边界。
A：我从验证后的 token 推导范围，各路候选在截断前过滤；检索内容按不可信数据渲染，输出再做结构与业务校验。删除先在事务内隐藏并阻断旧工作，再由带提交资格的 worker 清理权威与派生内容，留下非内容墓碑。
R：这些机制已有代码和测试，但不证明所有模型都不受注入。原始 source 在候选政策前已保留，备份、WAL 与外部 provider 也有独立治理范围，不能说全世界数据已经被擦除。

#### 追问一：Prompt Injection 能否只靠转义解决？

**STAR 追问口播**

S：即使恶意内容被正确编码在字符串里，它仍可以用自然语言要求模型忽略规则、泄露信息或调用不该调用的工具。
T：我需要防止格式逃逸，同时把模型可能误服从的影响限制在确定的权限和操作边界内。
A：我用结构隔离标记外部数据，授权与 scope 留在服务端，对输出验证类型、业务参数和允许资源。消费 Agent 每次工具执行还必须独立检查用户意图和权限；高风险动作不能只因为模型提议就被执行。真实行为防护另用固定模型攻击集验证。
R：转义解决的是一部分结构问题，不能根治语义注入。当前能说实现了结构边界和权限约束，不能说防住所有攻击，尤其不能把零有效回答下的零敏感输出解释成有用的安全能力。

#### 追问二：为什么向量库不能作为权威源？

**STAR 追问口播**

S：模型、切片或索引结构变化后，向量需要重算；如果系统只剩向量而没有可靠来源与状态，重建和纠正就失去依据。
T：我需要让事实、版本、权限和删除有一个可验证的权威边界，并让检索表示可以安全替换。
A：本项目用 PostgreSQL 中的证据、断言版本和状态转移作为权威，向量、全文与当前状态作为投影，附带身份、代次和进度约束。读取先遵守范围和真值，再使用投影找候选；回滚模型也创建新代次。
R：这让重建和删除落在明确的一致性边界。但不是说任何向量数据库都永远不能存权威数据，而是本项目选定的生命周期合同由关系权威实现，尚未证明迁往另一产品能保持相同事务语义。

## 8. 主题五：评测、交付与岗位反问

### Q13：模型没变 tag，但权重变了，怎样检测与回滚？

考察：身份与执行代次。证据：E12。L2。

**STAR 主问口播**

S：同一个模型标签可能指向新权重，即使维度没变，新旧向量空间也未必可比较，词法通道还可能漏过旧投影。
T：我需要检测配置漂移并阻止陈旧工作发布，同时保留权威事实与可解释的回滚路径。
A：我把可变请求标签和完整 digest 分开记录，启动校验模型身份、能力和维度；不一致时拒绝或执行明确的维护式代次切换。新代次重建当前权威状态，旧 job 的 owner 和 generation 都不能继续发布。
R：已有数据库测试覆盖切换、重复执行、旧代拒绝及回滚，回到旧模型也产生新代次。当前是维护流程，重建期间召回可能不完整，不是零停机双索引切换，也不包括任意同代索引损坏的自动修复。

#### 追问：A→B→A 为什么不能只比较模型名字？

**STAR 追问口播**

S：第一次使用 A 时产生的旧任务可能延迟很久才恢复，系统此时已经从 B 回到 A，单比名字会误以为它仍有发布资格。
T：我需要区分模型内容相同与任务所属构建轮次相同，避免旧快照覆盖当前合法投影。
A：我让模型身份描述向量空间，让递增构建代次描述本次工作资格；提交同时检查代次、当前权威状态和租约。回滚仍从当前 retained authority 重建，不能直接复用所有旧任务与缓存。
R：这样能在机制上隔离旧执行轮次，也让故障测试具备明确的拒绝条件。代次跳号不代表丢事实，事务回滚后的序列空洞可允许；真正需要核验的是当前选择、重建意图和发布状态是否保持一致。

### Q14：四基线如何保证公平？

考察：共享回答路径、预处理与成本。证据：E13、E15。L2→L3。

**STAR 主问口播**

S：MemOS、全文、摘要与纯向量各自处理历史的方式不同，若同时改变模型、回答提示和预算，分数差就无法归因。
T：我需要让比较回答“记忆策略有什么代价和收益”，并把方法固有差异公开，而不是偷偷给某条路径更多资源。
A：我固定数据版本、split、公共回答模型与提示、token 预算、超时和重试；各方法必要的摘要、提取、embedding 与计数调用分别冻结并记录用量。每个预期样本都有状态，失败不能悄悄重跑到成功。
R：已发布包能校验配置、覆盖和报告重建，但同公共回答 Prompt 不等于所有预处理 Prompt 必须一样。公平也不是零成本差异，而是所有差异都可见、可解释，且没有把后台提取成本漏掉。

#### 追问：给输出 Schema 加引用枚举，会不会偏袒 MemOS？

**STAR 追问口播**

S：真实运行发现模型会引用上下文中的记忆或版本身份，而 harness 曾只接受源事件身份，甚至还会出现 UUID 抄写错误。
T：我需要修复输入输出的身份合同，避免把协议不一致混成质量劣势，同时防止为某条基线单独放宽规则。
A：我让公共回答路径按各自实际可见证据生成允许引用集合，并将允许的记忆、版本或源身份归回数据集来源，未知引用仍拒绝。无证据情况还需要合法的空引用 Schema，并把配置变化记录成新版本。
R：开发 smoke 的执行问题得到修复，但合法引用不等于引用能支持答案。原冻结结果保留，新规则不能用于回改历史成绩，未来比较要让所有基线遵守同一公共引用合同。

### Q15：Recall、MRR、准确率、写入精确率分别测什么？

考察：定义、分母与指标误导。证据：E14、E15。L2。

**STAR 主问口播**

S：检索到了相关片段，答案仍可能错；模型猜对答案，也不一定说明用了正确证据，所以单一总分会掩盖失败阶段。
T：我需要分别评价写入是否污染、需要的证据是否出现、排名是否靠前，以及最终回答是否满足任务标签。
A：我对写入接受集合计算误报和漏报，对前 K 证据计算召回，对首个相关结果计算倒数排名，再单独算回答与引用。实际代码中冻结字段 recall_at_k 是任一 gold 命中率，另有全部 gold 命中指标，我会主动披露，不能按名称背定义。
R：当前准确率采用预声明的归一化精确匹配，不是人工语义裁判；多跳题和同义正确答案存在额外解释边界。完整写入 precision/recall 尚未建立，重复三次也不增加独立问题数。

#### 追问：分母选错会怎样？空结果算安全成功吗？

**STAR 追问口播**

S：如果只统计成功执行的样本，超时和格式错误会从报表消失；如果把每次无输出都算安全成功，无用系统反而可能得到最好分数。
T：我需要保证每项指标对应完整的预期集合，并把效用和安全一起展示，避免选择分母制造结论。
A：我会先声明按独立题、重复执行还是被接受候选统计，再单独列失败、缺失、排除与适用条件。安全题同时看攻击违规、正常请求误拒和需要拒答的命中情况；多证据题单独检查证据是否完整。
R：冻结 MemOS 零正确时的零敏感输出不构成有用防护，开发三题全对也不足以估计总体准确率。任何百分比都应能回到原始行和适用分母，而不只展示一张总分表。

### Q16：讲一个最能体现工程判断的失败案例。

考察：STAR 是否有真实证据。证据：E15。L2→L3。

**STAR 主问口播**

S：首轮真实开发评测中，Java 服务与 Python runner 各自测试通过，联调却因为 tokenizer 身份不一致而失败。
T：我需要判断是模型变化还是协议假设错误，在不降低身份校验强度的前提下恢复可比较实验。
A：我对照实际 Java 返回与 Python 预期，发现一侧包含明确的 embedding 身份前缀，另一侧只期待 digest；先纠正 fake 使错误假设出现红灯，再修 runner 并保留错误前缀与错误 digest 仍被拒绝的回归。
R：修复让真实公共合同一致，而不是把身份检查删除。原失败包保留，后续冻结评测可以完成但质量仍为负。这个案例说明两边 mock 一起写错也能全绿，端到端验证必不可少，它是本地实验故障，不是线上事故。

#### 追问：另外两个案例是什么，哪一个还没彻底解决？

**STAR 追问口播**

S：另外一个早期失败是模型提出非法半开时间区间，另一个是任务成功但所有候选被忽略或隔离，没有可检索记忆。
T：我需要区分格式、语义、政策和业务结果，不能把三种情况都归成“LLM 不稳定”。
A：对时间问题我保留严格区间约束并单独版本化提取提示；对零记忆问题，我查看 accepted 数、政策原因和权限传播，后来修复能力丢失及提取定义，再在开发集验证。每一步都保留原结果，不从观察推断每条拒绝都错误。
R：目前开发链路恢复，但真实写入与冲突分类的泛化质量仍未彻底建立。三个案例可以定位源码和 artifact，却不能被讲成生产成功率提升或个人独立解决所有模型问题。

### Q17：项目大量使用 AI，你的个人价值怎么核验？

考察：Ownership Claim，禁止代填个人经历。证据：提交与本人现场操作。L1→L3。

**STAR 主问口播**

S：这个项目有 AI 协作实现和文档生成，仓库功能、提交作者与个人真实掌握不能简单画等号。
T：我需要清楚说明自己负责的范围、实际判断和验收方式，而不是把上游或 AI 完成的全部工作自动记为独立主导。
A：正式面试时我会先填写本人能解释和修改的模块、对应提交以及尚不能独立解释的部分，再现场沿一个输入追到事务、状态与测试，修改一个小行为并给出反例。AI 参与的设计、代码和材料也明确说明。
R：现在文档和测试证明项目存在与机制可运行，不证明我的掌握程度。只有经过现场解释、修改和验证的部分，我才使用负责或实现等职责表述；尚未完成的个人核验保持待确认，不用背稿掩盖。

#### 追问：现在不用 AI 改一个功能，你会选什么？

**STAR 追问口播**

S：面试官希望看到候选人能否把架构理解落到可控修改，而不是只知道目录和术语。
T：我会选择边界清晰、能在短时间验证、不会触碰真实数据的任务，并提前说明当前版本与预期行为。
A：例如在现有测试环境增加一种不应触发检索的输入，或者修正一个身份校验错误映射，先写出正常与反例，再修改消费层的规则，运行对应回归并检查没有扩大权限。如果自己不能完成，也会直接承认并定位知识缺口。
R：这是建议的能力验证题，不是已经完成的个人实绩。通过标准是能解释为何改这里、为什么反例会失败、如何恢复原行为，而不是测试数量增加或代码能编译就算掌握核心链路。

### Q18：D21 的 KEEP/DEMOTE 怎么判断？

考察：结果导向和退出纪律。证据：E16。L3。

**STAR 主问口播**

S：项目最容易陷入不断增加功能，却始终没有真实实验、消费场景或本人理解证据的循环。
T：我需要一个到期可以执行的判断，让后续投入取决于可验证价值，而不是已经花了多少时间。
A：我要求正式四基线有完整可重建 artifact、环境可复现、失败案例可定位，并至少有真实消费者或集成演示。结果不优于简单基线时允许弱化保留，主要讲机制和负结果，个人职责另行核验；缺结果或无法运行就降为可选模块。
R：当前本地技术证据支持弱化保留，Waku 接口消费者已经存在，但不能代替真实用户长期使用、本人现场掌握或新质量优势。到了期限仍缺这些，就收缩叙事和投入，不靠再加功能延期。

#### 追问：为了求职，是否应该等项目完全优化完再投？

**STAR 追问口播**

S：后端与 AI 应用岗位需要的是能理解需求、解决具体问题并交付，而项目永远可能有新的实验或治理边界要补。
T：我需要把求职准备和项目深化并行，避免用“还没优化到极致”拖延真实岗位反馈。
A：我会先按已有证据投递匹配的 Java 或 AI 应用后端岗位，用真实工作经历支撑业务部分，用 MemOS 展示机制与评测；同时只补影响任务闭环的最高优先级缺口。岗位要求推理底层或生产 Agent 经验时，就坦诚差距并调整目标。
R：这是基于当前材料的定位建议，不保证录用或薪资。新一轮优化是否值得做，应由目标 JD、面试反馈和用户任务失败共同决定，不由代码规模、流行技术或完美主义决定。

### Q19：为什么冻结测试这么差，是项目没开发好吗？

考察：归因精度与开发/测试隔离。证据：E05、E13、E15。L2→L3。

**STAR 主问口播**

S：冻结集 MemOS 零比三十，确实说明当时真实模型组合没有交付有效答案，但不等于全部工程机制都没有实现。
T：我需要解释质量为什么失败，并把可确认的软件缺陷、模型表现和实验测量问题分别定位，不能简单甩锅模型。
A：我从无证据回答追到没有 accepted memory，查出角色未传成 source 写能力，以及小模型对持久事实和噪声的提取定义不足；修复后的开发运行又暴露引用身份合同与抄写问题，随后做公共约束修复。
R：开发三题恢复为三比三，Waku 接口测试通过，但引用缺陷不能被倒推成原冻结零分的全部原因。旧结果主要反映没有形成可用记忆，新结果只证明开发路径修复，不证明未见质量已经反转。

#### 追问：为什么不把原 test 再跑一遍，拿更好的成绩？

**STAR 追问口播**

S：工程师已经看过旧测试题、标签与失败模式，后续修复多少会受这些信息影响，即使没有逐题硬编码也不再是完全未知评测。
T：我需要既允许修复真实缺陷，又保护质量结论的可信度，不让开发反馈伪装成泛化结果。
A：旧测试保持不可变，如果重跑只能明确标记回归或事后分析；正式新结论需要新冻结配置和独立留出的任务族，先声明指标、预算、失败处理与人工验收，再运行。不能一边看结果一边删难题，也不能把重复生成样例当独立未见标签。
R：目前可说开发修复通过，不能说原冻结实验已经被推翻。负结果本身是可解释的工程证据，下一轮是否改进必须由新的有效实验回答，而不是靠更漂亮的报告替换历史。

### Q20：Waku 接入难在哪里，只是调用 HTTP 吗？

考察：同步消费合同、更新与错误语义。证据：E17。L2。

**STAR 主问口播**

S：Waku 的语义存储方法让调用方期待写完能读、更新后看新值、删除后查不到，而 MemOS 的受理和模型物化是异步的。
T：我需要把两边的可见性、版本与错误合同对齐，而不是只让请求返回成功状态码。
A：适配层写入后等待 source settlement，读取只返回进入上下文的证据；更新先证明替换来源产生版本，再用最新 ETag 失效旧版本，删除走治理接口。实际 conformance 覆盖命中、miss、列表、更新、删除与非英文输入。
R：固定版本的十二个真实后端用例通过。但更新由多个请求组成，不是原子事务，部分失败仍可能留下新数据；某些读取异常还会转为空列表，这些必须作为下一轮失败合同验证重点，不夸大为生产接入完全解决。

#### 追问：后端坏了也返回空列表，为什么危险？

**STAR 追问口播**

S：空列表可能代表没有相关记忆，也可能代表认证过期、超时或响应错误。上层 Agent 如果分不清，会在故障时继续回答甚至重复写入。
T：我需要保持可用的退化行为，同时让调用方能知道证据缺失是正常业务状态还是基础设施失败。
A：下一轮我会在独立 scope 注入超时、未授权和坏响应，检查适配层结果与可观测事件，再决定是否增加明确错误分类、状态通道或调用方兜底。更新部分成功也要记录操作身份并定义重试，不能仅把返回 false 当作什么都没发生。
R：这项完善当前是源码识别出的风险及验证计划，不是已完成的修复。现有 happy-path conformance 不能覆盖所有错误传播，真正退出条件应是失败可见、状态可解释且重试不会扩大错误。

### Q21：你如何保证以后还能接 Waku？

考察：版本兼容与保证范围。证据：E17。L2→L3。

**STAR 主问口播**

S：两个项目分别演进，API、字段、身份和同步等待语义都可能变化，一次联调成功不能自动保证未来任意版本兼容。
T：我需要把可承诺范围固定到版本与合同，并让破坏兼容的变更在发布前可见。
A：当前记录双方提交、模型身份与启动步骤，用共享语义存储 conformance 验证 MemOS 适配路径；后续发布需要运行相关合同与失败场景，接口演进先考虑兼容窗口与迁移，再决定切换。数据库和模型迁移也遵守相同的身份约束。
R：目前能确认固定 Waku 提交下十二项真实后端测试通过，完整仓库 gate 为六百一十九通过、七十三项外部条件跳过。不能把跳过算通过，也不能承诺未来未验证版本永远自动兼容，持续保证来自持续执行的发布门槛。

#### 追问：接口合同通过了，真实 Agent 任务为什么还可能失败？

**STAR 追问口播**

S：接口测试可以验证写读更新删除，却未必覆盖模型是否在正确时机调用记忆、是否遵守新事实以及最终工具动作是否完成用户任务。
T：我需要把存储机制验证与用户任务验收分成两层，不用底层测试数量代替产品结果。
A：下一轮可以选一个跨会话项目约束任务，固定模型和工具范围，让 Agent 记住、应用、纠正、删除后再次执行，记录实际工具输入、来源与最终产物，同时与无长期记忆和默认存储比较。失败按检索、决策、工具执行与环境分类。
R：这个业务闭环目前仍需单独的运行证据和人工验收。Waku 是真实代码消费者，但不因此自动出现真实活跃用户，更不能把其上游全部 Agent 功能作为我个人独立实现的成果。

### Q22：针对目标岗位，还应该继续深入优化什么？

考察：投入排序与岗位适配。证据：导学第十一节、E17。L3。

**STAR 主问口播**

S：当前项目的机制深度已经足以引出事务、状态、安全与评测追问，继续增加模型或图数据库未必改善求职匹配。
T：我需要围绕 Java 后端和 AI 应用岗位补最影响可信度的证据，同时避免把应用工程经历包装成训练或推理底层能力。
A：我会优先验证 Waku 错误语义和更新部分完成，再做一个真实任务闭环、独立未见评测和代表负载测量。纯后端岗位同时补并发、SQL、线程连接资源与真实工作场景；Agent 岗位补工具权限、取消恢复和任务验收。
R：我的判断是值得有边界地深化，也已经可以开始匹配投递。没有瓶颈就暂缓扩容，没有质量消融就暂缓重排和图；具体优化要有问题、实验与退出条件，不能靠新技术名词弥补尚未验证的产品价值。

#### 追问：如果只能再投入一周，你怎样安排？

**STAR 追问口播**

S：时间有限时，完整生产治理和大规模实验都做不完，随意扩范围最容易留下多个半成品。
T：我需要在一周内交付一条可演示、能解释失败、可重复验收的消费路径，同时保留诚实的未完成项。
A：前两天定义并验证一个 Waku 任务及失败矩阵，中间只修影响它的最高两类根因，随后冻结配置做留出验证，最后整理原始证据、五分钟演示与个人现场修改练习。如果未见集来不及有效建立，就明确提交开发回归和待测协议，不冒充正式评测。
R：这是一周预算建议而非新期限承诺，也不改变原 D21 的把关日期。最终产物应是可审查的任务结果和边界说明，不是“支持更多功能”的清单；达不到就收缩到可选 Memory 后端。

## 9. 源码证据索引

表内路径相对项目根目录；内部名称集中在此处，口播优先使用通用工程语言。

| 证据 | 关键路径与内部符号 | 对应正文 |
|---|---|---|
| E01 | [README](README.md)、[问题定义](docs/architecture/01-problem-definition.md) | Q1、Q10 |
| E02 | [推荐架构](docs/architecture/03-recommended-architecture.md)、`memory-domain` | Q2 |
| E03 | [SourceIngestionService](modules/ingestion/src/main/java/dev/memos/ingestion/SourceIngestionService.java)、[JdbcSourceIngestionStore](modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcSourceIngestionStore.java) | Q4、Q6 |
| E04 | [StrictCandidateProposalDecoder](modules/materialization/src/main/java/dev/memos/materialization/StrictCandidateProposalDecoder.java) | Q3 |
| E05 | [DeterministicCandidateWritePolicy](modules/governance/src/main/java/dev/memos/governance/DeterministicCandidateWritePolicy.java)、[V010](modules/adapters/src/main/resources/db/migration/V010__source_write_capabilities.sql) | Q3、Q12、Q19 |
| E06 | [OutboxWorkerService](modules/materialization/src/main/java/dev/memos/materialization/OutboxWorkerService.java)、[worker 测试](modules/materialization/src/test/java/dev/memos/materialization/OutboxWorkerServiceTest.java) | Q4、Q5、Q9 |
| E07 | [TemporalTransitionPlanner](modules/memory-domain/src/main/java/dev/memos/domain/temporal/TemporalTransitionPlanner.java)、[JdbcTemporalMemoryAuthority](modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcTemporalMemoryAuthority.java) | Q6、Q10、Q11 |
| E08 | [HybridRetrievalService](modules/retrieval/src/main/java/dev/memos/retrieval/HybridRetrievalService.java)、[JdbcRetrievalCandidateStore](modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcRetrievalCandidateStore.java) | Q7、Q8 |
| E09 | [MemoryContextAssembler](modules/context/src/main/java/dev/memos/context/MemoryContextAssembler.java) | Q9、Q14 |
| E10 | [JwtActorContextResolver](applications/memos-api/src/main/java/dev/memos/api/security/JwtActorContextResolver.java)、[AuthenticatedActor](applications/memos-api/src/main/java/dev/memos/api/security/AuthenticatedActor.java) | Q8、Q12 |
| E11 | [JdbcDeletionStore](modules/adapters/src/main/java/dev/memos/adapters/postgres/JdbcDeletionStore.java)、[删除并发测试](modules/adapters/src/test/java/dev/memos/adapters/postgres/JdbcDeletionStoreIntegrationTest.java) | Q12 |
| E12 | [V009](modules/adapters/src/main/resources/db/migration/V009__projection_reconciliation.sql)、[ADR 0006](docs/adr/0006-projection-identity-reconciliation.md) | Q13 |
| E13 | [runner](benchmark/src/memos_benchmark/runner.py)、[Ollama client](benchmark/src/memos_benchmark/ollama.py)、[MemOS client](benchmark/src/memos_benchmark/client.py) | Q14、Q19 |
| E14 | [metrics](benchmark/src/memos_benchmark/metrics.py)、[artifacts](benchmark/src/memos_benchmark/artifacts.py) | Q15 |
| E15 | [三个真实失败](docs/evidence/failure-cases.md)、[完整结果](docs/benchmark/results.md)、[报告重建](docs/evidence/reproduction.md) | Q1、Q9、Q15、Q16、Q19 |
| E16 | [21 天门槛](docs/evidence/21-day-gate.md) | Q18 |
| E17 | [消费方证据](docs/evidence/waku-integration-2026-09-07.md)、[Waku 固定源码](https://github.com/Zhi-Xiang-Guo/waku-agent/blob/b75adf280aab4456d123693dcc3c942ed4f73dcc/waku/memory/semantic/memos_store.py) | Q20、Q21、Q22 |

## 10. 面试评分与复练

L1 通过：不用内部名词堆叠，讲清一个具体输入、一个输出和一条边界。L2 通过：画出事务或状态时序，
能指出一个失败条件及已有回归。L3 通过：提出备选、反例和可证伪实验，说明成本与仍未验证的假设。
没有用个人回答实际检验前，不给“已掌握”结论。遇到卡顿，先降到最小问题，例如“202 保证了什么”。

推荐第一轮 Q1→Q4→Q10→Q12→Q19；第二轮 Q5→Q6→Q11→Q13→Q20；第三轮 Q7→Q14→Q15→Q21→Q22。
一次一问，不要求一次背完全部。技术答案是参考，不要求逐字一致；虚构职责、指标或未实现机制需要立即纠正。

## 11. 交接事实摘要

交给 /great-resume：项目是 MemOS，目标为 Java 后端 / AI 应用工程；个人职责待现场核验。
可写候选是事务 outbox、状态治理、范围过滤、投影代次与可重建评测、固定 Waku 接入；
禁止写生产规模、质量显著提升、成本下降、推理优化或独立主导全部。结果以 E15/E17 为准。

交给 /interview：最高风险 Ownership 是 AI 协作与上游边界；Metric 是把 30 次重复当 30 个独立问题，
把 3/3 开发正确当泛化优势；Architecture 是把维护重建说成无缝切换、把多请求更新说成原子；
Result 是把接口消费者说成生产用户、把空结果说成安全成功。复练必须要求具体源码、反例与本人修改证据。
